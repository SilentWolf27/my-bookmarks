import { LoginForm } from "@/components/auth/LoginForm";

export default function LoginPage() {
  return (
    <main className="w-full px-6 py-12 text-base flex flex-col justify-center items-center">
      <LoginForm />
    </main>
  );
}
